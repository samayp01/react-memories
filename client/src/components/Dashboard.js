import React, { Component } from 'react'

import styles from './css-modules/dashboard.module.css'

export default class Dashboard extends Component {
    render() {
        return (
            <div className={styles.sampleDash}>
                welcome
            </div>
        )
    }
}